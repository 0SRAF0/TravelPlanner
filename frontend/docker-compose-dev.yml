name: dev_travel-planner-frontend
services:
  web:
    container_name: web
    env_file:
      - .env.dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_API_BASE_URL: ${VITE_APP_API_BASE_URL}
    ports:
      - "3060:80"
    restart: unless-stopped

