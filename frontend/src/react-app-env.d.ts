/// <reference types="vite/client" />
/// <reference types="react" />
/// <reference types="react-dom" />

// This file ensures the editor/TypeScript server loads the correct React/Vite types for JSX.
// It is intentionally minimal; keep it at src/ so the tsconfig "include": ["src"] picks it up.

